// Preload runs in a sandboxed context before the page loads.
// Keep this minimal — only expose what the renderer genuinely needs.
// Currently nothing is needed: the app is a pure shell around the web URL.
